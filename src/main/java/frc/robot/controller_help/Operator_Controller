
OPERATOR CONTOLLER

Buttons:

A = Climber To Bottom
B = NOTHING
X = Climb (AFTER USING "Y"!!)
Y = Intake Up And Climber to top

Dpad:

Down = Climber Down (Manual) {DO NOT USE DURING MATCH ONLY BEFORE AND AFTER WHEN NEEDED}
Up = Climber Up (Manual) {DO NOT USE DURING MATCH ONLY BEFORE AND AFTER WHEN NEEDED}
Left = NOTHING
Right = NOTHING

Bumpers:

Left = run outtake
right = NOTHING

Triggers:

Left = Jostle
Right = Ferry (Dont Use Until Hood)

Menu Buttons:

Start = NOTHING
Back = Reset Climber